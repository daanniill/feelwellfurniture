# ===================================
# PUBLIC Environment Variables
# (Exposed to browser - safe for client-side)
# ===================================

# Fillout Form Field IDs (Public - safe to expose)
REACT_APP_FILLOUT_FIELD_FULLNAME=wqYk
REACT_APP_FILLOUT_FIELD_EMAIL=3WFW
REACT_APP_FILLOUT_FIELD_PHONE=8tLm
REACT_APP_FILLOUT_FIELD_ADDRESS=v6Ch
REACT_APP_FILLOUT_FIELD_NOTES=bLma
REACT_APP_FILLOUT_FIELD_CART=iduf
REACT_APP_FILLOUT_FIELD_TOTAL=gpzH

# Contact Form Field IDs (Public - safe to expose)
REACT_APP_FILLOUT_CONTACT_FIELD_NAME=your_field_id
REACT_APP_FILLOUT_CONTACT_FIELD_EMAIL=your_field_id
REACT_APP_FILLOUT_CONTACT_FIELD_PHONE=your_field_id
REACT_APP_FILLOUT_CONTACT_FIELD_SUBJECT=your_field_id
REACT_APP_FILLOUT_CONTACT_FIELD_MESSAGE=your_field_id

# ===================================
# PRIVATE Environment Variables
# (Server-side only - NEVER expose in browser)
# ===================================

# Fillout API Configuration (PRIVATE - server-side only)
FILLOUT_API_KEY=your_api_key_here
FILLOUT_FORM_ID=your_form_id_here
FILLOUT_CONTACT_FORM_ID=your_contact_form_id_here

# ===================================
# IMPORTANT SECURITY NOTES:
# ===================================
# 1. Variables prefixed with REACT_APP_ are exposed to the browser
# 2. Variables WITHOUT REACT_APP_ prefix are server-side only
# 3. NEVER prefix API keys with REACT_APP_
# 4. Field IDs are safe to expose (they're just references)
# 5. API keys and Form IDs must stay server-side only
